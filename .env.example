# Database
DATABASE_URL=postgresql://user:password@host:5432/lions_nursery

# NixOS: use `nix-shell` (see shell.nix). Run `prisma` / `npm run db:*` inside that shell —
# do not use nixpkgs `prisma` (v7) or bare `npx` if your PATH has no Node.

# NextAuth v5
AUTH_SECRET=
AUTH_URL=https://lionslandscapenursery.com
ADMIN_EMAIL=admin@lionslandscapenursery.com

# Gmail OAuth2 (production email)
# Dev: omit GMAIL_CLIENT_ID to auto-create an Ethereal inbox (credentials print in terminal).
# Prod: set all four vars below. Obtain them from Google Cloud Console + OAuth Playground.
# Guide: https://developers.google.com/oauthplayground/ (use scope: https://mail.google.com/)
GMAIL_USER=your-email@gmail.com
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
AUTH_EMAIL_FROM="Lions Nursery" <noreply@lionslandscapenursery.com>

# Cloudinary
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=

# SEO
GOOGLE_SITE_VERIFICATION=

# Vercel / production
# Set install/build command to: npm run build:vercel
# After first deploy, run: npm run db:seed (with production DATABASE_URL)
